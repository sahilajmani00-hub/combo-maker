# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Higgsfield API credentials written by the AI section's setup form
.env

# Default destination for AI angle-combo runs
/ai-combos/

# Points at the queue folder the browser extension is working through
/.active-queue

# Saved product setups — your own photos, not part of the tool
/templates/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nested clone of the earlier imagecombomaker repo — not part of this project
/imagecombomaker/

# Last Drive upload's manifest, so the listing sheet survives a restart
/.last-drive-run.json

# Private hosted accounts, invitation hashes, and sessions
/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
hosted-dist
sites-dist
